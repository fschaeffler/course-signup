'use strict';

describe('courseSignupApp.course-signup module', function() {

  beforeEach(module('courseSignupApp.course-signup'));

  describe('course-signup controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var course-signupCtrl = $controller('View1Ctrl');
      expect(course-signupCtrl).toBeDefined();
    }));

  });
});